# 更新日志

## 2026-07-16

- 将样式配置改为右侧独立面板，默认收起，保留原有主题和样式作为兜底。
- 字体列表保留 Windows 常用字体，并精简内置外部字体为阿里巴巴普惠体 3.0、思源黑体风格和思源宋体风格；思源字体仅打包 WOFF2，降低安装包体积。
- 增加计划任务 / 待办清单的醒目渲染规则，支持 `- [ ]` 待执行与 `- [x]` 已完成，并纳入元素覆盖样式配置。
- 接入可视化编辑模式，保留预览、可视化编辑、分栏编辑三种模式，并修复 Mermaid 渲染、主题色、滚动位置和源码同步问题。
- 分栏编辑支持拖动中间分割线调整左右宽度。
- 非雅致模式下左侧目录高度改为占满可用区域，不影响雅致模式阅读布局。
- 视图模式切换改成更直观的 `预览 / 编辑 / 分栏` 标签式切换。
- 移除前端“设置为默认程序”入口，避免不稳定的文件关联交互。
- 新增应用设置弹窗，可维护模型配置，并可控制主题、缩放、视图模式、目录宽度、分栏宽度是否保存。
- 主题切换、内容缩放、视图模式、目录宽度、分栏宽度支持按用户开关缓存。
- 左上角目录按钮移动到打开按钮左侧；保存按钮恢复为仅在内容修改后显示。
- 新增“智能排版”功能，可调用当前启用模型整理 Markdown 排版；失败或返回内容不安全时不会覆盖原文，并允许切换其他启用模型重试。
- 模型配置新增“测试模型”功能，测试未通过或配置变更后的模型会自动禁用，不能设为当前模型或参与智能排版。
- 智能排版成功后新增左右分栏预览确认弹窗，左侧显示原内容，右侧显示排版后内容，用户确认后才会应用到文档。
- 文件监听改为自动同步：当前文档未修改时，外部编辑器保存后会直接覆盖查看器内容，不再要求用户手动刷新。
- 新增双向编辑冲突保护：仅当本软件存在未保存编辑且磁盘文件也被修改时触发，并在标题右侧显示状态；用户可选择保留当前编辑并覆盖磁盘，或加载外部最新版本。
- 智能排版无可用模型时，设置弹窗会自动切换到“模型配置”页面。
- Windows 窗体改为无原生标题栏的一体化布局，内置最小化、最大化/还原和关闭按钮；标题栏及系统边框随主题切换，不再额外设置应用内容圆角。
- 修复可视化编辑中拖选多行后 `Delete` / `Backspace` 偶尔无法删除的问题。
- 修复文件冲突选择版本后仍显示保存按钮、后续外部修改再次误报冲突的问题。
- 智能排版的排版前后渲染预览支持按内容进度同步滚动。
- 新增多文件与文件夹工作区：可通过“打开”多选文件、通过“文件夹”载入多层目录，或直接拖入多个文件和目录；左侧“文件 / 大纲”页签支持目录树展开收起和点击切换当前文件。
- 扩展常见文本格式支持：Markdown 继续使用完整渲染与可视化编辑，TXT、日志、配置、代码等文本使用保留空白的安全预览和源码编辑；自动跳过常见二进制文件。
- 文本读取与保存兼容 UTF-8、UTF-8 BOM、UTF-16 和 GB18030，并尽量保持文件原编码。
- 恢复 Windows WebView2 原生 `Ctrl+滚轮` 整体界面缩放，与右上角内容缩放相互独立。
- 可视化编辑模式移除“待执行 / 已完成”状态文字，避免与可编辑正文重叠；预览中的任务状态徽标继续保留。
